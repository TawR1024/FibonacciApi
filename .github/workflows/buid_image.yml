name: Docker

on:
  push:
    branches:
      - dev

  env:
    # TODO: Change variable to your image's name.
    IMAGE_NAME: fibonacciApi

jobs:
  push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build image
        run: docker build . --file Dockerfile --tag image